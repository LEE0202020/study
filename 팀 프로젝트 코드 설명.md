# 팀 프로젝트 크롤링 코드 설명

대상 파일: `팀 프로젝트 .ipynb`

실제로 실행해서 결과를 낸 **금감원(FSS) 검색어 필터 크롤러**를 기준으로 한 줄씩 설명합니다. 금융위(FSC) 코드도 구조가 거의 같아서, 마지막에 차이점만 정리했습니다.

---

## 1. 준비 (임포트 + 기본 설정)

```python
import requests                # 웹페이지에 HTTP 요청을 보내는 라이브러리
from bs4 import BeautifulSoup  # 받아온 HTML을 파싱(분석)하는 라이브러리
import pandas as pd            # 표 형태(DataFrame)로 데이터를 정리하기 위해
import re                      # 정규표현식 (문자열에서 숫자 등을 뽑아낼 때)
import time                    # 요청 사이에 잠깐 쉬기 위해

HEADERS = {
    "User-Agent": "Mozilla/5.0 (...) Chrome/120.0.0.0 Safari/537.36"
}
```

`User-Agent`는 "나는 크롬 브라우저다"라고 알려주는 헤더입니다. 이게 없으면 일부 사이트는 프로그램(봇)의 요청으로 인식해서 차단하기도 합니다.

---

## 2. 목록 페이지 한 장 읽기: `fss_search_list()`

```python
def fss_search_list(keyword, page_index=1, sdate="", edate=""):
    url = f"{FSS_BASE_URL}/fss/bbs/B0000188/list.do"
    params = {
        "menuNo": "200218", "bbsId": "", "cl1Cd": "",
        "pageIndex": page_index, "sdate": sdate, "edate": edate,
        "searchCnd": 1, "searchWrd": keyword,
    }
    res = requests.get(url, params=params, headers=HEADERS, timeout=10)
```

- 주소창에 넣었던 긴 URL(`?menuNo=...&searchWrd=...`)을 그대로 문자열로 붙이는 대신, `params` 딕셔너리로 넘겼습니다. `requests`가 자동으로 `?menuNo=200218&pageIndex=1&...` 형태로 조립해줍니다. 이렇게 하면 나중에 `keyword`나 `page_index`만 바꿔서 재사용하기 쉽습니다.
- `res.raise_for_status()`는 요청이 실패(404, 500 등)하면 바로 에러를 내서, 잘못된 페이지를 계속 진행하지 않게 막는 안전장치입니다.

```python
    soup = BeautifulSoup(res.text, "html.parser")
```

받아온 HTML 텍스트(`res.text`)를 BeautifulSoup 객체로 바꿉니다. 이제부터 `.select()`, `.select_one()` 같은 CSS 선택자로 원하는 태그를 찾을 수 있습니다.

### 전체 페이지 수 읽기

```python
    spans = soup.select(".total-count span")
    page_text = spans[1].get_text(" ", strip=True) if len(spans) > 1 else ""
    m = re.search(r"/\s*(\d+)", page_text)
    total_pages = int(m.group(1)) if m else 1
```

페이지에 "전체 394건 / 페이지 1 / 40" 같은 문구가 있는데, 그중 `.total-count` 안의 두 번째 `span`(`"페이지 1 / 40"`)에서 `/` 뒤의 숫자(40)를 정규표현식으로 뽑아냅니다. 이렇게 하면 "몇 페이지까지 있는지" 코드가 직접 알아내서, 사람이 페이지 수를 미리 셀 필요가 없습니다.

### 게시글 한 줄(행)씩 파싱

```python
    rows = []
    for tr in soup.select("table.tbl.col.list-data tbody tr"):
        title_tag = tr.select_one("td.title a")
        if not title_tag:
            continue
```

목록은 `<table class="tbl col list-data">` 안에 `<tr>`(행) 10개가 들어있는 구조입니다. `tr` 하나하나가 게시글 한 건입니다. 제목 링크(`<a>`)가 없는 행(광고나 빈 행 등)은 `continue`로 건너뜁니다.

```python
        tds = tr.find_all("td")
        rows.append({
            "번호": tds[0].get_text(strip=True),
            "제목": title_tag.get_text(strip=True),
            "담당부서": tds[2].get_text(strip=True),
            "등록일": tds[3].get_text(strip=True),
            "조회수": tds[-1].get_text(strip=True),
            "링크": FSS_BASE_URL + title_tag["href"],
        })
    return rows, total_pages
```

한 행 안의 `<td>`들을 순서대로 꺼냅니다 (실제 HTML을 확인해서 0번=번호, 1번=제목, 2번=담당부서, 3번=등록일, 마지막(-1번)=조회수인 걸 알아냈습니다). `title_tag["href"]`는 `/fss/bbs/.../view.do?...`처럼 **도메인이 빠진 상대경로**라서, 앞에 `FSS_BASE_URL`(`https://www.fss.or.kr`)을 직접 붙여줘야 실제로 접속 가능한 주소가 됩니다.

---

## 3. 상세 페이지에서 본문만 뽑기: `fss_get_detail_content()`

```python
def fss_get_detail_content(detail_url):
    res = requests.get(detail_url, headers=HEADERS, timeout=10)
    res.raise_for_status()
    soup = BeautifulSoup(res.text, "html.parser")

    body_tag = soup.select_one(".krds-bd-view .n-dbdata")
    return body_tag.get_text("\n", strip=True) if body_tag else ""
```

목록에서 얻은 링크 하나(`detail_url`)로 실제 상세 페이지에 다시 접속합니다. 상세 페이지의 본문은 `<div class="krds-bd-view">` 안의 `<div class="n-dbdata">`에 들어있다는 걸 HTML을 직접 열어서 확인했습니다. 그 태그의 텍스트만 뽑아서 돌려줍니다. 태그가 없으면(구조가 다른 예외 페이지) 빈 문자열을 반환해서 에러 없이 넘어갑니다.

---

## 4. 전체를 순회하는 지휘자: `fss_search_crawl()`

```python
def fss_search_crawl(keyword, sdate="", edate=""):
    all_rows = []
    page = 1
    total_pages = 1
    while page <= total_pages:
        rows, total_pages = fss_search_list(keyword, page, sdate, edate)
```

처음엔 `total_pages`를 모르니 일단 1로 두고 `while` 문을 돕니다. 1페이지를 읽자마자 진짜 `total_pages`(40)가 갱신되고, 그 뒤로는 `page`가 40이 될 때까지 자동으로 반복됩니다. **페이지 수를 하드코딩하지 않은 이유**는, 나중에 새 보도자료가 올라와서 페이지 수가 바뀌어도 코드 수정 없이 그대로 동작하게 하기 위해서입니다.

```python
        for row in rows:
            row["본문"] = fss_get_detail_content(row["링크"])
            all_rows.append(row)
            time.sleep(1)
```

한 페이지에서 얻은 10개 게시글 각각에 대해 상세 페이지를 다시 요청해서 본문을 채워 넣습니다. `time.sleep(1)`은 요청 사이 1초씩 쉬는 부분인데, 너무 빠르게 연속 요청하면 정부 사이트에서 디도스(DDoS)성 트래픽으로 오인해 IP를 차단할 수 있어서 넣은 예의상 지연입니다.

```python
        page += 1
    return pd.DataFrame(all_rows)
```

딕셔너리 리스트(`all_rows`)를 마지막에 `pd.DataFrame()`으로 감싸면, 엑셀 표처럼 행/열 구조를 가진 데이터로 바뀝니다.

---

## 5. 실행과 저장

```python
fss_search_df = fss_search_crawl("불공정거래")
fss_search_df
```

함수를 실제로 호출하는 부분입니다. `"불공정거래"`가 URL의 `searchWrd=불공정거래`에 해당하는 검색어입니다.

```python
fss_search_df.to_csv("금감원_보도자료_불공정거래.csv", index=False, encoding="utf-8-sig")
```

| 옵션 | 의미 |
|---|---|
| `index=False` | DataFrame의 왼쪽 번호(0, 1, 2...)는 저장하지 않음 |
| `encoding="utf-8-sig"` | 엑셀에서 열었을 때 한글이 깨지지 않도록 BOM을 붙인 UTF-8로 저장 |

---

## 6. 전체 흐름 한눈에 보기

```
fss_search_crawl("불공정거래")
│
├─ 1페이지 요청 → fss_search_list()
│     ├─ 전체 페이지 수(40) 확인
│     └─ 목록 10건 파싱 (제목/날짜/조회수/링크)
│
├─ 방금 얻은 10건 각각에 대해
│     └─ fss_get_detail_content(링크) → 본문 텍스트 채우기 (요청마다 1초 대기)
│
├─ 2페이지 요청 → 위 과정 반복
│     ...
├─ 40페이지까지 반복 (total_pages에 도달하면 종료)
│
└─ 모든 행을 pd.DataFrame으로 묶어서 반환
        │
        └─ to_csv()로 파일 저장
```

---

## 7. 금융위(FSC) 코드와의 차이

구조(목록 읽기 → 상세 본문 읽기 → 여러 페이지 순회 → 저장)는 동일하고, 아래만 다릅니다.

| 구분 | 금융위(FSC) | 금감원(FSS) |
|---|---|---|
| 목록 선택자 | `.board-list-wrap ul li` | `table.tbl.col.list-data tbody tr` |
| 제목 선택자 | `.subject a` | `td.title a` |
| 본문 선택자 | `.board-view-wrap .body .cont` | `.krds-bd-view .n-dbdata` |
| 페이지 순회 방식 | `pages=(2,)`처럼 페이지 번호를 직접 지정 | 전체 페이지 수를 자동으로 읽어서 끝까지 순회 |
| 검색 필터 | 없음 (특정 게시판 전체) | `searchWrd` 키워드로 필터링 |

두 사이트가 서로 다른 HTML 구조를 쓰기 때문에, 같은 논리(목록 파싱 → 링크 따라가기 → 본문 파싱)라도 CSS 선택자만 사이트에 맞게 바꿔서 재사용한 형태입니다.

---

## 8. 코드를 짤 때 사용한 방법 (참고)

선택자(`.board-view-wrap .body .cont` 같은 것)는 추측한 게 아니라, 실제 웹페이지의 HTML을 직접 내려받아서 확인한 뒤 정한 것입니다.

1. `curl`로 목록 페이지 HTML을 파일로 저장
2. HTML 안에서 `class="..."` 패턴을 찾아 후보를 추림
3. 실제 게시글이 들어있는 태그 구조(표인지 리스트인지, 제목이 어느 클래스에 있는지)를 눈으로 확인
4. 상세 페이지도 같은 방식으로 확인해서 본문 위치를 특정
5. 확정한 선택자로 코드를 작성한 뒤, 소량(1~2페이지)으로 먼저 실행해서 결과가 맞는지 검증
6. 검증되면 전체 범위(40페이지)로 확장해서 실행
