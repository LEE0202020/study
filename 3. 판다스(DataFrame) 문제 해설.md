# 3. 판다스(DataFrame) 노트북 문제 해설

대상 파일: `3. 판다스(DataFrame) .ipynb` (타이타닉 데이터셋, 891행 x 12열)

- Part A: 노트북의 실습 문제 해설
- Part B: 노트북에 있는 오류·버그와 수정 방법

---

## Part A. 실습 문제 해설

### A-1. 데이터 파악 (shape / describe / info)

| 코드 | 의미 | 결과 |
|---|---|---|
| `df.shape` | (행, 열) | (891, 12) |
| `df.size` | 행 x 열 = 전체 원소 수 | 10692 |
| `df.ndim` | 차원 | 2 |
| `len(df)` | 행 개수 | 891 |
| `df.describe()` | 숫자 컬럼 통계 (count, mean, std, min, 25/50/75%, max) | Age의 count가 714 |
| `df.info()` | 컬럼별 타입과 non-null 개수 | Age 714, Cabin 204, Embarked 889 |

`info()`의 non-null 개수가 891보다 작은 컬럼은 결측치(NaN)가 있다는 뜻입니다. 나이(177개), 객실번호(687개), 탑승항구(2개)가 비어 있습니다.

### A-2. dict -> DataFrame

```python
data = {"이름": "홍길동", "나이": 20, "주소": "서울시 강남"}
pd.DataFrame(data, index=[0])
```

- dict의 값이 스칼라(단일 값)이면 행 인덱스를 알 수 없어서 `index=[0]`을 반드시 줘야 합니다. 안 주면 `ValueError: If using all scalar values, you must pass an index`가 납니다.
- 값이 리스트(Series 형태)이면 길이로 행 수가 정해지므로 `index`를 생략해도 됩니다.

### A-3. 컬럼 선택: `df["Name"]` vs `df[["Name"]]`

| 코드 | 결과 타입 |
|---|---|
| `df["Name"]` | Series (1차원) |
| `df[["Name"]]` | DataFrame (2차원) |

대괄호를 한 겹 더 씌우면 "컬럼 이름의 리스트"를 넘기는 것이라 DataFrame이 유지됩니다.

### A-4. 행 슬라이싱: `df[0:3]` vs `df.loc[:3]`

| 코드 | 끝 값 | 결과 행 |
|---|---|---|
| `df[:3]` | 포함 안 함 | 0, 1, 2 (3개) |
| `df.loc[:3]` | 포함함 | 0, 1, 2, 3 (4개) |

- `df[a:b]`는 파이썬 리스트처럼 위치 기준이고 끝을 포함하지 않습니다.
- `loc[a:b]`는 라벨 기준이고 끝을 포함합니다.

### A-5. loc / iloc

| | 기준 | 슬라이스 끝 | 음수 인덱스 |
|---|---|---|---|
| `loc` | 라벨(인덱스 이름) | 포함 | 라벨 `-1`이 없으면 KeyError |
| `iloc` | 위치(0부터) | 미포함 | `-1`이 마지막 행 |

노트북의 핵심 예제:

```python
df[::10].loc[10]   # 라벨 10인 행 (PassengerId 11)
df[::10].iloc[1]   # 위치 1번째 행 -> 같은 행 (라벨 0, 10, 20, ... 중 두 번째)

df[::5].loc[100]   # 라벨 100 -> PassengerId 101, Age 28.0
df[::5].iloc[100]  # 위치 100 -> 라벨 500 -> PassengerId 501, Age 17.0
```

`[::5]`처럼 행을 건너뛰어 뽑으면 라벨과 위치가 어긋나므로 `loc`과 `iloc`의 결과가 달라집니다.

다중 선택:

```python
df.loc[[1, 600, 800]]                      # 여러 행
df.loc[1, "Name"]                          # [행, 열] 한 칸
df.loc[[100, 200, 300], ["Name", "Age"]]   # 여러 행 x 여러 열
```

### A-6. Boolean Selection

```python
age_mask = df["Age"] > 30        # True/False로 이루어진 Series
df[age_mask]                     # True인 행만 남김 -> 305행
```

- 마스크는 조건식의 결과(bool Series)이고, `df[마스크]`로 행을 걸러냅니다.
- 조건을 여러 개 묶을 때는 `and`/`or` 대신 `&`/`|`를 쓰고, 각 조건을 괄호로 감쌉니다.
- 요금 400 초과: 3명 (모두 Fare 512.3292, Ticket `PC 17755`).

### A-7. 문제: 여성 생존자 조회와 생존율

```python
survived_female_mask = (df["Survived"] == 1) & (df["Sex"] == "female")
survived_female = df[survived_female_mask]      # 233명
female = df[df["Sex"] == "female"]              # 314명
print(f"생존율: {len(survived_female) / len(female) * 100:.2f}%")   # 74.20%
```

- 분자는 "여성이면서 생존한 사람"(233)이고, 분모는 "여성 전체"(314)입니다.
- 분모가 여성 전체이므로 이 경우에는 올바른 계산입니다. (Part B-3에서 이 패턴을 잘못 재사용한 사례를 다룹니다.)

### A-8. 문제: 1등석 vs 3등석 생존율

가장 간단한 정답은 `groupby`입니다.

```python
df.groupby("Pclass")["Survived"].mean()
```

| Pclass | 생존율 | 생존자 / 전체 |
|---|---|---|
| 1 | 62.96% | 136 / 216 |
| 2 | 47.28% | 87 / 184 |
| 3 | 24.24% | 119 / 491 |

- `Survived`가 0/1이라서 평균이 곧 생존율입니다.
- `.agg(["mean", "count"])`로 평균과 인원수를 같이 볼 수 있습니다.
- 노트북에서는 결과 Series의 인덱스를 `["1등석", "2등석", "3등석"]`으로 바꾼 뒤 `DataFrame(data=시리즈 * 100, index=[...])`으로 만들었습니다. `data`가 Series일 때 `index`를 넘기면 그 라벨에 맞춰 재정렬되므로, Series 인덱스와 `index=` 값이 정확히 같아야 NaN이 생기지 않습니다.

### A-9. 파생 변수

파생 변수는 기존 컬럼으로 새 컬럼을 만든 것입니다.

```python
df["RealAge"] = df["Age"] - 1         # 없는 컬럼명에 대입 -> 새 컬럼 추가 (전체에 벡터 연산)
```

```python
def get_adult(age):
    if np.isnan(age):
        return "알 수 없음"
    return "성인" if age >= 19 else "아이"

df["IsAdult"] = df["Age"].apply(get_adult)
```

- `np.nan == np.nan`은 False라서 NaN 판별에는 `np.isnan()` (또는 `pd.isna()`)을 써야 합니다.
- `.apply(함수)`는 Series의 각 값을 함수에 하나씩 넣고 결과를 모아 새 Series로 돌려줍니다.
- 조건이 세 가지 이상이면 `if ~ elif ~ else`로 나눕니다.

```python
df["RipOff"] = df["Fare"].apply(lambda fare: "바가지" if fare > 300 else "정상")
```

한 줄짜리 간단한 규칙은 `lambda`로 처리합니다. 요금이 300 초과인 승객 3명만 "바가지"가 됩니다.

### A-10. 컬럼 삭제: `drop`

```python
df.drop("Cabin", axis=1)                              # 복사본만 삭제 (원본 유지)
df.drop(["Cabin", "Embarked"], axis=1)                # 여러 개는 리스트로
df.drop(["Cabin", "Embarked"], axis=1, inplace=True)  # 원본에서 실제로 삭제
```

- `axis=0`(기본)은 행, `axis=1`은 열입니다.
- `inplace=False`(기본)는 삭제된 복사본을 반환할 뿐 원본은 그대로입니다. 결과를 변수에 받거나 `inplace=True`를 써야 실제로 반영됩니다.

---

## Part B. 오류·버그 상세

### B-1. SyntaxError: `pd.Series` 3개를 만드는 셀

원본:

```python
pd.Series(data=["홍길동", 20, "경기도 수원"], index= ["이름", "나이", "주소 "]
pd.Series(data=["장보고", 30, "경기도 분당"], index= ["이름", "나이", "주소 "]
pd.Series(data=["이순신", 40, "경기도 의정부"], index= ["이름", "나이", "주소 "]

pd.DataFrame([a, b, c])
```

문제 세 가지:
1. 각 줄 끝의 `)`가 빠져 있어서 문법 오류(`Perhaps you forgot a comma?`)가 납니다.
2. Series를 `a`, `b`, `c`에 대입하지 않았는데 `pd.DataFrame([a, b, c])`에서 사용합니다. 고쳐도 NameError가 납니다.
3. 인덱스 `"주소 "`에 공백이 있습니다. 이 상태로 만들면 컬럼명이 "주소 "가 되어 `df["주소"]`로 접근할 수 없습니다.

수정:

```python
a = pd.Series(data=["홍길동", 20, "경기도 수원"], index=["이름", "나이", "주소"])
b = pd.Series(data=["장보고", 30, "경기도 분당"], index=["이름", "나이", "주소"])
c = pd.Series(data=["이순신", 40, "경기도 의정부"], index=["이름", "나이", "주소"])
pd.DataFrame([a, b, c])
```

Series 여러 개를 리스트로 넣으면 각 Series가 한 행이 되고, Series의 인덱스가 컬럼명이 됩니다.

### B-2. NameError와 그로 인한 잘못된 결과 (가장 중요)

원본 (`# ______썜__________` 셀):

```python
survived_mask = titanic["Survived"] == 1
titanic_df[survived_mask]
print(f"타이타닉호의 생존률: {len(titanic_df[survived_mask])/len(titanic_df) * 100:.2f}%")
```

**1단계: 오류.** `titanic`은 정의된 적 없는 변수입니다. 데이터프레임 이름은 `titanic_df`입니다. 그래서 `NameError: name 'titanic' is not defined`가 납니다.

**2단계: 연쇄 피해.** 이 셀은 첫 줄에서 실패하기 때문에 `survived_mask`가 새로 만들어지지 않았습니다. 그런데 `survived_mask`라는 이름은 바로 위 "여성 생존자" 셀에서 이미 다음 값으로 만들어져 있었습니다.

```python
survived_mask = (df["Survived"] == 1) & (df["Sex"] == "female")   # 여성 생존자!
```

그 결과 다음 셀이 잘못된 마스크를 그대로 사용했습니다.

```python
pclass_1_survived = len(df[survived_mask & pclass_1_mask])   # 1등석 여성 생존자 = 91
pclass_3_survived = len(df[survived_mask & pclass_3_mask])   # 3등석 여성 생존자 = 72
```

노트북에 찍힌 값과 실제 값을 비교하면 다음과 같습니다.

| | 노트북 출력 (잘못됨) | 계산 | 올바른 값 (`groupby`와 일치) | 계산 |
|---|---|---|---|---|
| 1등석 | 42.13% | 91 / 216 | 62.96% | 136 / 216 |
| 3등석 | 14.66% | 72 / 491 | 24.24% | 119 / 491 |

같은 노트북 안에서 `groupby` 결과(62.96%, 24.24%)와 숫자가 다르다는 점이 버그의 증거입니다.

수정:

```python
survived_mask = df["Survived"] == 1     # titanic -> titanic_df 로 수정, 셀 재실행

pclass_1_mask = df["Pclass"] == 1
pclass_3_mask = df["Pclass"] == 3
print(f"1등석 생존률: {len(df[survived_mask & pclass_1_mask]) / len(df[pclass_1_mask]) * 100:.2f}%")  # 62.96%
print(f"3등석 생존률: {len(df[survived_mask & pclass_3_mask]) / len(df[pclass_3_mask]) * 100:.2f}%")  # 24.24%
```

교훈: 같은 이름의 변수를 다른 의미로 재사용하면 이런 오류가 조용히 넘어갑니다. `female_survived_mask`, `survived_mask`처럼 의미가 구분되는 이름을 쓰고, 에러가 난 셀을 고친 뒤에는 그 아래 셀을 반드시 다시 실행해야 합니다.

### B-3. 탑승 항구별 생존율의 분모 오류

원본:

```python
print(f"생존율: 약 {round(len(Survived_Embarked_s) / len(femail), 4) * 100}%")   # 69.11%
print(f"생존율: 약 {round(len(Survived_Embarked_c) / len(femail), 4) * 100}%")   # 29.62%
```

문제: 항구 S에서 탄 생존자(217명)를 `len(femail)`, 즉 여성 전체 수(314)로 나눴습니다. A-7의 코드를 복사하면서 분모를 바꾸지 않은 것입니다. 생존율의 분모는 "그 그룹의 전체 인원"이어야 합니다.

| 항구 | 생존자 / 전체 | 올바른 생존율 | 노트북 출력 |
|---|---|---|---|
| S (Southampton) | 217 / 644 | 33.70% | 69.11% (217/314) |
| C (Cherbourg) | 93 / 168 | 55.36% | 29.62% (93/314) |
| Q (Queenstown) | 30 / 77 | 38.96% | (계산 안 함) |

간단한 수정:

```python
df.groupby("Embarked")["Survived"].mean()
```

보조 문제:
- 이 셀들은 "1등석과 3등석의 생존율 비교" 제목 아래에 있지만 내용은 항구별 분석입니다. 제목 바로 아래 셀은 비어 있습니다.
- `round(x, 4) * 100`은 반올림을 한 뒤 곱해서 부동소수점 오차가 남을 수 있습니다. `f"{x * 100:.2f}"`처럼 서식으로 자리수를 지정하는 것이 안전합니다.

### B-4. `drop`이 실제로 삭제하지 않는 셀

원본 (마지막 실행 셀):

```python
titanic_df.drop(["IsAdult", "RipOff"], axis= 1)
# titanic_df.drop(["IsAdult", "RipOff"], axis= 1, inplace=True) # = 완전 삭제
```

주석에 "두 개를 모두 삭제"라고 적었지만, 실행되는 첫 줄은 `inplace`가 없어서 삭제된 복사본을 화면에 출력할 뿐입니다. `titanic_df`에는 `IsAdult`와 `RipOff`가 그대로 남아 있습니다. 실제로 지우려면 아래 줄의 주석을 풀거나, 다음처럼 재대입합니다.

```python
titanic_df = titanic_df.drop(["IsAdult", "RipOff"], axis=1)
```

### B-5. 의미 없는 `value_counts()`

원본:

```python
titanic_df.value_counts()
```

DataFrame 전체에 쓰면 "모든 컬럼 값이 완전히 같은 행"의 개수를 셉니다. 승객은 모두 `PassengerId`가 달라서 값이 전부 1입니다. 또 NaN이 있는 행은 기본적으로 제외되어 891행이 아닌 183행만 나옵니다. 컬럼 하나에 쓰는 것이 의도였을 가능성이 큽니다.

```python
titanic_df["Sex"].value_counts()    # male 577, female 314
```

### B-6. 주석·오타 (동작에는 영향 없음)

| 위치 | 문제 | 수정 |
|---|---|---|
| `RipOff` 주석 | `Fare <= 300 : 바가지 요금` | `Fare <= 300 : 정상` |
| `# 103행 데이터의 나이` | 코드는 `loc[100]`이라 101번째 행 (PassengerId 101) | 주석을 "101번째 행"으로 |
| 마크다운 | `털럼`, `인데스`, `colimn` | 컬럼, 인덱스, column |
| 컬럼 설명 | `Pork of Embarkation`, `Southampthon` | Port, Southampton |
| `Survived_female`/`femail` | 변수명 오타(`female`) | `female` |
| 변수 대소문자 | `Survived_Embarked_s_mask`처럼 대문자로 시작 | 파이썬은 소문자 `snake_case` 관례 |

### B-7. 실행 환경상 주의점

1. **셀 실행 순서.** `Cabin`, `Embarked`를 `inplace=True`로 삭제한 뒤, 위쪽의 `Embarked` 관련 셀을 다시 실행하면 `KeyError: 'Embarked'`가 납니다. 실행 번호(`In[89]` 등)가 뒤섞여 있으므로 결과가 이상하면 커널을 재시작하고 위에서부터 전체 실행하세요.
2. **절대경로.** `base_path = "/Users/kdt_0900_lyn/data_analysis/resource/dataset"`은 본인 컴퓨터에서만 동작합니다. 데이터도 저장소 밖(`resource/dataset`)에 있어서, GitHub에서 받은 다른 사람은 그대로 실행할 수 없습니다. 데이터를 저장소 안으로 옮기고 `./dataset/titanic.csv` 같은 상대경로를 쓰거나, README에 데이터 위치를 적어두는 것을 권장합니다.

---

## 요약 체크리스트

- [ ] B-1: `pd.Series` 셀 괄호·대입·공백 수정
- [ ] B-2: `titanic` -> `titanic_df` 수정 후 등석 생존율 재계산 (62.96% / 24.24%)
- [ ] B-3: 항구별 생존율의 분모를 그룹별 전체 인원으로 수정
- [ ] B-4: `IsAdult`, `RipOff` 삭제를 `inplace=True` 또는 재대입으로
- [ ] B-5: `value_counts()`를 컬럼 하나에 적용
- [ ] B-6: 주석·오타 정리
- [ ] B-7: 커널 재시작 후 전체 실행, 경로 정리
