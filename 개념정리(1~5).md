# 데이터 분석 개념 정리 (노트북 1~5)

| 번호 | 노트북 | 핵심 주제 |
|---|---|---|
| 1 | 넘파이(Numpy) | ndarray, shape/reshape, 브로드캐스팅, axis, boolean selection |
| 2 | 판다스(Series) | Series 생성·인덱싱, NaN, 연산, drop, groupby |
| 3 | 판다스(DataFrame) | DataFrame, loc/iloc, 마스크, groupby, 파생 변수 |
| 4 | 맷플로립(Matplotlib) | line/bar/hist/scatter, subplot, 스타일 |
| 5 | 데이터 분석(기초) | 빅데이터 5V, 계급·도수, 평균·편차·분산·표준편차 |

---

## 1. 넘파이 (Numpy)

### 1-1. 왜 넘파이인가

- 파이썬 데이터 분석의 기본 라이브러리이고, pandas·머신러닝 라이브러리의 바탕이 됩니다.
- 기본 타입은 `ndarray`(n차원 배열)입니다.
- 파이썬 list보다 빠르고 메모리를 적게 쓰며, 선형대수·통계 함수를 제공합니다.

### 1-2. 배열 생성

| 코드 | 설명 |
|---|---|
| `np.array([1, 2, 3])` | 리스트로 생성 (`np.ndarray([...])`가 아님) |
| `np.arange(10)`, `np.arange(1, 10, 2)` | `range`와 같은 규칙(끝 미포함)으로 생성 |
| `np.ones(8)`, `np.zeros(8, dtype=np.int64)` | 1 또는 0으로 채움. 기본 타입은 실수(float64) |
| `np.random.randint(1, 46, (2, 3))` | 정수 난수 (끝 미포함, 크기는 튜플) |
| `np.random.rand(n)` | 0~1 균등분포 난수 |
| `np.random.randn(n)` | 표준정규분포 난수 |
| `np.random.seed(값)` | 난수를 재현 가능하게 고정 (같은 seed면 같은 난수) |

**한 배열은 하나의 타입만 가집니다.** 섞이면 더 넓은 타입으로 자동 승격됩니다.

```python
np.array([1, 2, 3.0])              # float64  -> [1. 2. 3.]
np.array([1, 2, False, "홍길동"])   # 문자열(<U..)로 통일
```

### 1-3. 차원과 shape

| 구분 | 예시 | shape |
|---|---|---|
| 1차원 (벡터) | `[1, 2, 3]` | `(3,)` |
| 2차원 (행렬) | `[[1, 2, 3], [4, 5, 6]]` | `(2, 3)` |
| 3차원 (텐서) | `[[[..]]]` | `(면, 행, 열)` |
| 2차원 스칼라 | `[[2]]` | `(1, 1)` |

| 속성 | 의미 |
|---|---|
| `arr.shape` | 각 차원의 크기 (튜플) |
| `arr.ndim` | 차원 수 |
| `arr.size` | 전체 원소 개수 (shape의 곱) |
| `arr.dtype` | 자료형 |

### 1-4. 인덱싱 · 슬라이싱

```python
arr = np.arange(1, 11)
arr[1]        # 요소 하나 (np.int64)
arr[2:5]      # 위치 2~4 (끝 미포함)
arr[::2]      # 2칸씩
arr7[1][0][2] == arr7[1, 0, 2]   # 다차원 접근
```

### 1-5. 자료형 변환 `astype`

```python
arr.astype(np.int8)   # 복사본을 만들어 타입 변경 (원본 유지)
np.arange(10, dtype=np.uint8)   # u = unsigned(부호 없음)
```

### 1-6. shape 바꾸기

| 함수 | 역할 | 비고 |
|---|---|---|
| `reshape(a, b, ...)` | 모양 변경 | 원소 개수의 곱이 같아야 함. 안 맞으면 `ValueError` |
| `reshape(2, -1, 12)` | `-1`은 나머지 차원을 자동 계산 | |
| `ravel()` | 1차원으로 펴기 | 가능하면 원본의 view. `order='C'`(행 방향, 기본) / `'F'`(열 방향) |
| `flatten()` | 1차원으로 펴기 | 항상 복사본 |
| `np.expand_dims(arr, axis=0/1)` | 차원 추가 | `(10,)` -> `(1, 10)` 또는 `(10, 1)` |
| `squeeze()` | 크기 1인 차원 제거 | `(10, 1)` -> `(10,)` |
| `arr.T`, `np.transpose(arr)` | 전치 (행과 열 교환) | `(3, 5)` -> `(5, 3)` |

`np.unique(arr)`는 중복을 제거하고 정렬해서 돌려줍니다.

### 1-7. 배열 연산과 브로드캐스팅

- shape가 같으면 원소끼리 연산합니다.
- shape가 다르면 브로드캐스팅으로 크기를 맞춘 뒤 연산합니다.

**브로드캐스팅 규칙**
1. 뒤쪽 차원부터 비교합니다.
2. 각 차원의 크기가 같거나, 둘 중 하나가 1이면 통과입니다.
3. 하나라도 어긋나면 `ValueError`입니다.

```python
(4, 3) + (3,)     # OK   -> (4, 3)
(4, 3) + (4,)     # 오류 (뒤 차원 3 vs 4)
(2, 3) + (3, 2)   # 오류 -> 한쪽을 .T 로 바꾸면 OK
(6, 1, 5) + (7, 1)  # OK -> (6, 7, 5)
```

> 노트북의 메모에 `6 x 1 x 5`와 `7 x 3`이 어울리는 것처럼 적혀 있는데, 실제로는 마지막 차원 5와 3이 달라서 오류입니다. 짝은 `7 x 1`이어야 합니다.

### 1-8. axis (축)

- axis는 "그 방향으로 합쳐서 사라지는 차원"입니다.
- 2차원 `(3, 5)`에서 `axis=0`은 행을 합쳐 열별 결과(길이 5), `axis=1`은 열을 합쳐 행별 결과(길이 3)입니다.
- `axis=-1`은 마지막 차원입니다.

```python
a = np.arange(15).reshape(3, 5)
a.sum(axis=0)   # [15 18 21 24 27]
a.sum(axis=1)   # [10 35 60]
```

### 1-9. 최댓값 · 최솟값

| 함수 | 반환 |
|---|---|
| `arr.max()`, `arr.min()` | 값 |
| `arr.argmax()`, `arr.argmin()` | 그 값의 인덱스 (axis 없이 쓰면 펼친 배열 기준) |
| `arr.max(axis=0)`, `arr.argmax(axis=1)` | 축별 결과 |

### 1-10. Boolean Selection

```python
even_mask = arr % 2 == 0        # True/False 배열
arr[even_mask]                  # True인 것만
arr[(arr % 2 == 1) & (arr < 50)]   # 다중 조건: & (and), | (or), 각 조건은 괄호로
```

---

## 2. 판다스 Series

### 2-1. Series란

- 1차원 배열에 **인덱스(라벨)** 가 붙은 객체입니다. (index x value)
- 인덱스를 안 주면 0, 1, 2...가 자동으로 붙습니다.
- 값은 같은 타입입니다.

```python
pd.Series(data=[70, 80, 90], index=["홍길동", "장보고", "이순신"])
pd.Series({"한혜진": 1500000, "이상아": 50000})   # dict -> key가 index, value가 값
```

| 필드 | 의미 |
|---|---|
| `s.values` | 값 (ndarray) |
| `s.index` | 인덱스 |
| `s.dtype` | 자료형 |
| `s.name`, `s.index.name` | Series·인덱스의 이름 지정 |

### 2-2. 인덱싱 · 슬라이싱

| 코드 | 결과 | 비고 |
|---|---|---|
| `s["이예나"]` | 값 하나 (`numpy.int64`) | |
| `s[["이상아", "채진성"]]` | Series | 리스트로 여러 개 선택 |
| `s["오승현":"채진성"]` | Series | **라벨 슬라이싱은 끝을 포함** |
| `s[2:5]` | Series | 위치 슬라이싱은 끝 미포함 |
| `s["한혜진"] = -50000` | | 값 수정(원본 변경) |

### 2-3. 복사

```python
s4 = s3.copy()   # 독립된 복사본 (s4를 바꿔도 s3 유지)
```

`s4 = s3`은 같은 객체를 가리키기만 하므로 원본이 함께 바뀝니다.

### 2-4. Series 함수

| 함수 | 의미 |
|---|---|
| `ndim`, `size`, `shape`, `len(s)` | 차원, 원소 수(NaN 포함), `(n,)`, 길이 |
| `unique()` | 중복 제거한 값 (ndarray, NaN 포함) |
| `count()` | **NaN을 제외한** 개수 |
| `mean()` | NaN을 제외한 평균 |
| `value_counts()` | NaN을 제외한 값별 빈도 |
| `head(n)`, `tail(n)` | 앞/뒤 n개 (기본 5) |

**NaN(Not a Number)**: 숫자가 와야 하는 자리에 값이 없을 때의 결측값입니다. `np.nan == np.nan`은 False라서 `np.isnan()` / `pd.isna()`로 검사합니다. NaN이 있으면 정수 Series가 float으로 바뀝니다.

### 2-5. Series 연산

- **인덱스(라벨) 기준으로 정렬해서** 연산합니다. 위치가 아닙니다.
- 한쪽에만 있는 라벨은 결과가 NaN입니다.

```python
s2 + s3     # 겹치는 라벨만 계산, 나머지는 NaN
s2.add(s3, fill_value=0)   # 없는 쪽을 0으로 보고 계산 (NaN 방지)
```

### 2-6. 조건 필터링

```python
s2[s2 > 30000]                          # 숫자 조건
s4[s4.str.contains("호프|도박|흡연")]      # 문자열 포함 (| 는 or)
s4[~s4.str.contains("호프|도박|흡연")]     # ~ 는 반전 (== 0 대신 사용 가능)
```

### 2-7. drop

```python
s3.drop("a")                # 복사본 반환 (원본 유지)
s3.drop(["b", "c"])         # 여러 개
s3.drop("a", inplace=True)  # 원본에서 삭제 (반환값 None)
```

### 2-8. groupby

- 같은 값(라벨)끼리 묶어서 집계합니다.
- 인덱스에 **중복 라벨이 있는 Series**에서 특히 유용합니다.

```python
s1.groupby(s1.index).count()   # 이름별 개수
s1.groupby(s1.index).sum()     # 이름별 합
s1.groupby(s1.index).mean()    # 이름별 평균
s1.groupby(s1 >= 600).count()  # 조건(True/False)별 개수
```

### 2-9. 변환 함수

| 접두사 | 의미 |
|---|---|
| `to_...` | 형변환 (`to_list()`, `to_dict()`, `to_frame()`) |
| `is...` | bool 반환 |
| `get...` / `set...` | 값 읽기 / 쓰기 |

`to_dict()`는 키가 중복되면 마지막 값만 남습니다. (예: 인덱스에 "지혁"이 3번 있으면 마지막 값 하나만 유지)

---

## 3. 판다스 DataFrame

### 3-1. DataFrame이란

- 2차원 표(엑셀 시트, DB 테이블). 행(row)은 개별 데이터, 열(column)은 속성입니다.
- Series가 모인 구조이고, 각 열이 하나의 Series입니다.

### 3-2. 생성

```python
pd.DataFrame({"이름": "홍길동", "나이": 20}, index=[0])        # 스칼라 값은 index 필수
pd.DataFrame({"이름": ["홍길동", "장보고"], "나이": [20, 30]})   # 리스트 값이면 index 생략 가능
pd.DataFrame([series_a, series_b])   # Series 하나가 한 행, Series 인덱스가 컬럼명
pd.read_csv(path)                    # CSV 파일
```

### 3-3. 데이터 파악

| 코드 | 의미 |
|---|---|
| `df.shape` | (행, 열) |
| `df.size`, `df.ndim`, `len(df)` | 원소 수, 차원, 행 수 |
| `df.head(n)` | 앞 n행 |
| `df.describe()` | 숫자 컬럼의 통계 (count, mean, std, min, 25/50/75%, max) |
| `df.info()` | 컬럼별 타입과 non-null 개수 (결측 파악) |
| `df.index`, `df.columns` | 행/열 이름 |
| `df["컬럼"].value_counts()` | 컬럼 하나의 값별 빈도 |

### 3-4. 선택

| 코드 | 결과 |
|---|---|
| `df["Name"]` | Series |
| `df[["Name"]]`, `df[["Name", "Age"]]` | DataFrame |
| `df[0:3]` | 위치 기준 행 슬라이싱 (끝 미포함) |
| `df.loc[3]` | **라벨** 3인 행 (Series) |
| `df.iloc[3]` | **위치** 3번째 행 |
| `df.loc[:3]` | 라벨 0~3 (**끝 포함**) |
| `df.iloc[:3]` | 위치 0~2 (끝 미포함) |
| `df.loc[[1, 600], ["Name", "Age"]]` | 행 x 열 동시 선택 |
| `df.iloc[-1]` | 마지막 행 (`loc[-1]`은 KeyError) |

행을 건너뛰어 뽑은 뒤(`df[::5]`)에는 라벨과 위치가 어긋나므로 `loc`과 `iloc` 결과가 달라집니다.

### 3-5. Boolean Selection

```python
mask = (df["Survived"] == 1) & (df["Sex"] == "female")
df[mask]
```

- `and`/`or` 대신 `&`/`|`를 쓰고 조건마다 괄호로 감쌉니다.
- 같은 변수명을 다른 조건으로 다시 쓰면 오류가 조용히 넘어갈 수 있으니 이름을 구분합니다.

### 3-6. groupby / agg

```python
df.groupby("Pclass")["Survived"].mean()                # 등석별 생존율
df.groupby("Pclass")["Survived"].agg(["mean", "count"])
df.groupby(["Pclass", "Sex"])["Survived"].mean()       # 여러 기준
```

`Survived`가 0/1이라서 평균이 곧 비율(생존율)입니다. 비율의 분모는 항상 **그 그룹의 전체 인원**이어야 합니다.

### 3-7. 파생 변수

```python
df["RealAge"] = df["Age"] - 1                        # 벡터 연산으로 새 컬럼
df["IsAdult"] = df["Age"].apply(get_adult)           # 함수를 각 값에 적용
df["RipOff"] = df["Fare"].apply(lambda f: "바가지" if f > 300 else "정상")
```

- 없는 컬럼명에 대입하면 새 컬럼이 만들어집니다.
- `apply(함수)`는 Series의 값을 하나씩 함수에 넣어 결과를 모읍니다.
- NaN 판별은 `np.isnan()`을 씁니다.

### 3-8. 삭제 `drop`

```python
df.drop("Cabin", axis=1)                              # 복사본 (원본 유지)
df.drop(["Cabin", "Embarked"], axis=1, inplace=True)  # 원본 삭제
```

`axis=0`(기본)은 행, `axis=1`은 열입니다. `inplace=False`가 기본이라 결과를 변수에 받지 않으면 원본은 그대로입니다.

---

## 4. 맷플로립 (Matplotlib)

### 4-1. 기본

```python
import matplotlib.pyplot as plt
plt.plot(data)
plt.show()
```

한글 깨짐 방지 (맥 / 윈도우 중 사용하는 쪽):

```python
plt.rcParams['font.family'] = 'AppleGothic'     # 윈도우: 'Malgun Gothic'
plt.rcParams['axes.unicode_minus'] = False       # 마이너스 기호 깨짐 방지
```

### 4-2. 차트 종류

| 차트 | 함수 | 용도 |
|---|---|---|
| 라인 플롯 | `plt.plot(y)`, `plt.plot(x, y)` | 시간·순서에 따른 변화 |
| 바 차트 | `plt.bar(x, height)`, `s.plot(kind="bar")` | 항목별 크기 비교 |
| 히스토그램 | `plt.hist(data, bins=...)` | 도수 분포 (가로: 계급, 세로: 도수) |
| 산점도 | `plt.scatter(x, y)` | 두 변수의 관계 |
| 박스 플롯 | `plt.boxplot(data)` | 분포와 이상치 |

### 4-3. 꾸미기

| 코드 | 의미 |
|---|---|
| `plt.figure(figsize=(10, 5))` | 그림 크기 (가로, 세로 인치) |
| `plt.title("제목")` | 제목 |
| `plt.xlabel("X축")`, `plt.ylabel("y축")` | 축 이름 |
| `plt.legend(labels)` | 범례 |
| `plt.grid(True)` | 격자 |
| `color="pink"`, `marker="*"`, `linestyle="-."` | 색, 점 모양, 선 스타일 |

**2차원 배열을 `plt.plot`에 넣으면 열(column)마다 선이 한 개씩 그려집니다.** (행마다 그리려면 `.T` 또는 반복문)

`plt.plot(test_df)`처럼 DataFrame을 넣으면 컬럼별로 선이 그려지고, 인덱스가 x축이 됩니다.

### 4-4. subplot (여러 그래프를 한 그림에)

```python
fig = plt.figure(figsize=(8, 4))
ax1 = fig.add_subplot(2, 2, 1)   # (행 수, 열 수, 위치)
ax2 = fig.add_subplot(2, 2, 2)
ax1.hist(x1)
ax2.plot(x2)
```

`plt.plot`(pyplot 방식)은 현재 그림에, `ax.plot`(객체 방식)은 특정 칸에 그립니다.

### 4-5. 정규분포 난수

```python
np.random.normal(평균, 표준편차, size)   # 예: normal(0, 1, 1000)
```

---

## 5. 데이터 분석 기초

### 5-1. 데이터와 정보

- **데이터 = 사실(Fact)**, **정보 = 의미 있는 결과**, **데이터 분석 = 사실을 정보로 바꾸는 과정**
- 데이터 구분: 정량적 데이터(숫자로 측정), 정성적 데이터(설명·범주)

### 5-2. 빅데이터의 5V

| V | 의미 |
|---|---|
| Volume | 규모 |
| Velocity | 속도 |
| Variety | 다양성 |
| Veracity | 신뢰성 |
| Value | 가치 |

### 5-3. 빅데이터 시대의 패러다임 변화 (4가지)

| 과거 | -> | 현재 |
|---|---|---|
| 표본 조사 | -> | 전체 조사 |
| 사전 처리 | -> | 사후 처리 (있는 그대로 저장해 두고 나중에 활용) |
| 질(Quality) | -> | 양(Quantity) |
| 인과관계 | -> | 상관관계 |

배경: 인터넷·스마트폰 확산, 저장 비용 하락, 하둡 같은 분산 컴퓨팅의 발전.

### 5-4. 기초 통계 용어

| 용어 | 뜻 | 코드 |
|---|---|---|
| 변량 | 자료의 수치(값) | |
| 계급 | 변량을 일정 간격으로 나눈 구간 | `pd.cut(s, bins=[...])` |
| 도수 | 각 계급에 속하는 변량의 개수 | `groupby("계급").count()` |
| 상대도수 | 도수 / 전체 변량 수 (합이 1) | 도수 / `len(자료)` |
| 도수분포표 | 계급별 도수와 상대도수를 정리한 표 | |
| 히스토그램 | 도수분포표를 그린 그래프 | `hist(bins=...)` |

- `pd.cut`의 구간은 기본적으로 **오른쪽이 닫힌** `(a, b]` 형태입니다. `bins`의 최솟값과 같은 값은 어느 구간에도 들어가지 못해 NaN이 됩니다.
- 도수분포표는 구간별 분포를 한눈에 보기 좋지만 각 변량의 정확한 값은 사라집니다.

### 5-5. 산포도 (평균, 편차, 분산, 표준편차)

| 통계량 | 정의 | 코드 |
|---|---|---|
| 산술 평균 | 변량의 합 / 변량의 수 | `s.mean()` |
| 편차 | 변량 - 평균 (편차의 합은 항상 0) | `s - s.mean()` |
| 분산 | 편차 제곱의 평균 | `((s - s.mean())**2).mean()` |
| 표준편차 | 분산의 제곱근 | `np.sqrt(분산)` |

- 편차를 제곱하는 이유는 음수와 양수가 상쇄되지 않게 하기 위해서입니다.
- 표준편차가 작을수록 변량이 평균 가까이에 모여 있습니다.
- **주의:** 위 정의는 모분산(n으로 나눔)입니다. pandas의 `s.var()`, `s.std()`는 기본이 표본분산(n-1로 나눔, `ddof=1`)이라 값이 다릅니다. 같은 값을 얻으려면 `s.var(ddof=0)`을 씁니다.

> 노트북 5의 상대도수 계산에서는 자료가 10개인데 `/100`으로 나눠서 0.01, 0.03처럼 나왔습니다. 자료 수로 나눠야 하므로 `/10`(즉 `/len(자료)`)이 맞고, 그러면 합이 1이 됩니다.

---

## 자주 하는 실수 모음

| 실수 | 올바른 방법 |
|---|---|
| `np.ndarray([1, 2, 3])`로 배열 생성 | `np.array([1, 2, 3])` |
| reshape 크기 불일치 (`arange(24).reshape(5, 5)`) | 곱이 원소 수와 같아야 함 |
| `and`, `or`로 마스크 결합 | `&`, `\|`, 조건마다 괄호 |
| `drop` 후 원본이 바뀌었다고 생각 | 재대입하거나 `inplace=True` |
| `loc`/`iloc` 혼동 | `loc`=라벨(끝 포함), `iloc`=위치(끝 미포함) |
| 그룹 비율 계산 시 분모를 전체 인원으로 사용 | 분모는 그 그룹의 인원 |
| 에러 난 셀 아래를 재실행하지 않음 | 수정 후 아래 셀을 다시 실행 (이전 변수가 남아 잘못된 결과가 나올 수 있음) |
